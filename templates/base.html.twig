<!DOCTYPE html>
<html lang=fr>
    <head>
        <meta charset="UTF-8">
        <meta name="viewport"
              content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
        <meta http-equiv="X-UA-Compatible" content="ie=edge">
        <meta charset="UTF-8">
        <title>{% block title %}Tout mes livres{% endblock %}</title>
        <link rel="stylesheet" href="{{ asset('asset/css/style.css') }}">
        {% block stylesheets %}{% endblock %}
    </head>
    <body>
        {% include 'commons/_header.html.twig' %}
        <main>
            <div class="sticky">
                <h2><a href="{{ path('accueil') }}">Accueil</a></h2>
                <h2> <a href="{{ path('authors')}}">Les auteurs</a> </h2>
                <h2> <a href="{{ path('books')}}">Les livres</a> </h2>
            </div>
        {% block body %}

        {% endblock %}
        </main>
        {% include 'commons/_footer.html.twig' %}
        {% block javascripts %}{% endblock %}
    </body>
</html>
