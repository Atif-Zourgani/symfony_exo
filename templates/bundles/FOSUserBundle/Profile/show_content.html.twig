{% extends 'base.html.twig' %}
{% block body %}

{% trans_default_domain 'FOSUserBundle' %}
    <div class="displayAro">
        <div class="form fos_user_user_show">
            <p>{{ 'profile.show.username'|trans }}: {{ user.username }}</p>
            <p>{{ 'profile.show.email'|trans }}: {{ user.email }}</p>
        </div>
        <div class="listing">
            <a href="{{ path('fos_user_profile_edit') }}" ><h3>Modifier mon profil</h3></a>
        </div>
    </div>

{% endblock %}
